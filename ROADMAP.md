# Roadmap

- Complete the Todoist API client and enable write operations.
- Add the planned Docker packaging target.

The schedule API key and Todoist token are installation-level configuration,
never repository constants. Secret scanning and the local configuration ignore
rules guard that boundary.
